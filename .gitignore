node_modules/
dist/
*.log
.DS_Store

# per-deployment config (use sync_vault_config.example.json as template)
config/sync_vault_config.json
sync_vault_config.json

# local state / audit
*.jsonl
.audit/
status.json
